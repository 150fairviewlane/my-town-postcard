I have three files to add or replace. Save PostcardSampleAds.jsx at src/PostcardSampleAds.jsx as a new file. Replace src/AdGenerator.jsx with the updated version provided. Replace src/industryAssets.js with the updated version provided. All three files must be in the same src/ directory. Now update PostcardSpotPicker.jsx as follows. Step 1: Add this import at the top: import { getSampleAd, SPOT_SAMPLE_MAP } from ‘./PostcardSampleAds’; Step 2: Find the function that renders each spot cell in the postcard grid. Step 3: For spots that are already purchased by a paying customer, keep rendering their actual ad data as before. Step 4: For spots that are available and not yet purchased, replace whatever placeholder is currently shown such as the green plus sign or gray box with a call to getSampleAd. Look up the sample ad using the spot ID like this: const sampleAdKey = SPOT_SAMPLE_MAP[spot.id]; const sampleContent = sampleAdKey ? getSampleAd(sampleAdKey, spot.size) : null; Then in your JSX inside the available spot cell render the sample content inside a div with position absolute inset 0 and opacity 0.92, and if there is no sample content fall back to your existing empty state. Step 5: The spot IDs in SPOT_SAMPLE_MAP inside PostcardSampleAds.jsx use placeholder names like front-xl-1 and back-l-1. Look at the actual spot IDs used in your SPOTS array in PostcardSpotPicker.jsx and update the SPOT_SAMPLE_MAP keys in PostcardSampleAds.jsx to match them exactly. Step 6: Available spots must still be clickable and open the AdGenerator. The sample ad is purely decorative and sits behind a hover overlay. Wrap the spot content in a div with position relative width 100 percent height 100 percent and the existing click handler. Inside that div place the sample ad in an absolutely positioned div covering the full area. Then add a second absolutely positioned div covering the full area with a class of spot-hover-overlay that has background rgba 0 180 0 0.15, opacity 0, and transition opacity 0.2s, containing a centered green Reserve This Spot badge. Add CSS so that spot-hover-overlay shows opacity 1 on hover, either via a CSS class or a React hover state. Step 7: Do not change any backend code, Stripe integration, database logic, routing, or authentication. Only modify the visual rendering of available spots in PostcardSpotPicker.jsx and add the three new or updated files.