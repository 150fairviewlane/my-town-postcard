Three distinct issues from real generated ads — investigate and report on each separately before fixing, since they may have different causes:

1. TEXT COLLISION (highest priority — this is a functional defect, not aesthetic): on a Miguel's Tires ad (at-your-service template), the QR code's backing card visibly overlaps and cuts off part of the address text ("Clarkesville, G[A]" — the state gets clipped). Find where footer text positioning and QR card positioning are each computed, and check whether they account for each other's actual width/position, or are computed independently without checking for overlap. This likely isn't related to today's corner-decoration work at all — check if this is a pre-existing issue across other templates/sizes too, or specific to this one.

2. EMPTY SPACE ABOVE QR (check against today's last change): another Miguel's Tires ad (cream/navy/gold template) has a large blank, undecorated gap above the QR code where a colored panel/band would be expected. Check whether the generous erase-layer radius from the last change is erasing legitimate background decoration on this template, not just the area where a hallucinated shape might appear. If so, the erase radius likely needs to be template-aware rather than one global generous value — large enough to cover hallucinations on templates that need it, without eating real design on templates where the corner area is supposed to have visible content.

3. GLOW LOOKS LIKE AN UNWANTED SHAPE ON LIGHT BACKGROUNDS: a third Miguel's Tires ad (light cream background) shows what looks like a "random dark circle" around the QR. Check whether this is actually Grok-hallucinated content, or whether it's the server's own glow disc — which may read as a soft blend on dark/saturated template backgrounds but as a harsh, visible circle on light ones. If it's the latter, the glow's color/opacity likely needs to adapt to each template's actual background brightness rather than using one approach everywhere.

Separately, as a positive reference: the Big Ed's Bait & Tackle ad (brush-stroke template) came out great — the paint-splatter motif extends naturally into the corner near the QR and blends well with the glow. Identify what's different about this case (template style, glow color/background match, erase radius) that made it work, since that's the actual target outcome for the other templates too.

Report findings for all three before proposing fixes. Don't generate test ads — manual review only.
