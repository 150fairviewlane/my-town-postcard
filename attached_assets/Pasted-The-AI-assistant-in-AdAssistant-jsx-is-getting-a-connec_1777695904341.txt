The AI assistant in AdAssistant.jsx is getting a connection error because browser-side code cannot access the ANTHROPIC_API_KEY secret directly. I need you to add a simple backend proxy route that forwards requests to the Anthropic API server-side where the key is available.

STEP 1 - Add a proxy route to the Express server. Add a new POST route at /api/ai/chat. This route should accept a JSON body containing two fields: messages (an array of message objects with role and content) and system (a string with the system prompt). The route should forward these to the Anthropic API at https://api.anthropic.com/v1/messages using the ANTHROPIC_API_KEY from process.env. Use this exact fetch call on the server side:

const response = await fetch(“https://api.anthropic.com/v1/messages”, {
method: “POST”,
headers: {
“Content-Type”: “application/json”,
“x-api-key”: process.env.ANTHROPIC_API_KEY,
“anthropic-version”: “2023-06-01”
},
body: JSON.stringify({
model: “claude-haiku-4-5-20251001”,
max_tokens: 400,
system: req.body.system,
messages: req.body.messages
})
});
const data = await response.json();
res.json(data);

Wrap it in a try-catch and return a 500 with an error message if it fails. Do not add any authentication to this route for now.

STEP 2 - Update AdAssistant.jsx. Find the fetch call inside the sendMessage function that currently calls https://api.anthropic.com/v1/messages directly. Replace that URL with /api/ai/chat. Remove the Content-Type header since it is already the default for fetch with a JSON body — but keep the body the same. The updated fetch should look like this:

const response = await fetch(”/api/ai/chat”, {
method: “POST”,
headers: { “Content-Type”: “application/json” },
body: JSON.stringify({
system: buildSystemPrompt(formData, sizeKey),
messages: history,
}),
});

STEP 3 - Test it. Open the ad generator, type a message in the assistant panel, and confirm you get a real response instead of the error message. If you still get an error, check that ANTHROPIC_API_KEY is set in Replit secrets and that the server route is correctly reading process.env.ANTHROPIC_API_KEY.

Do not change anything else in AdAssistant.jsx or AdGenerator.jsx. Only add the proxy route to the server and update the fetch URL in AdAssistant.jsx.