Two bugs found when searching "Ball Ground, GA" on the Find Your Territory screen. The result shown was the EXISTING claimed Cherokee territory (Milton, Woodstock, Holly Springs, Canton) — not a new proposal centered on Ball Ground.

**Bug 1: Wrong county listed.** The result displays "Cherokee · Pickens" as the counties, but none of the 4 listed cities (Milton, Woodstock, Holly Springs, Canton) are actually in Pickens County — they're in Cherokee/Fulton. Investigate where this county label is computed from:
- Is it derived from the actual 4 selected hub cities' real counties (which it should be), or from something broader like the full search-radius footprint's geographic extent (which could brush into Pickens at its edges even if no actual hub city is there)?
- Check the specific function that builds the displayed `counties` list for this proposal and confirm whether it's reading from hub-city locations specifically, or from a wider ZIP/footprint area that isn't representative of where the actual selected hubs are.

**Bug 2: Searched city not included in result.** The user searched specifically for "Ball Ground, GA" — a real town, and one that ALREADY EXISTS as a hub city in a different, custom-built Cherokee-area territory created earlier today via the admin Custom Territory tool. The returned proposal did not include Ball Ground at all; instead it returned the SAME EXISTING claimed territory (Milton/Woodstock/Holly Springs/Canton).

Investigate:
1. Is the propose endpoint correctly treating "Ball Ground" as the search anchor, or is something causing it to fall back to/collide with the nearby already-claimed Cherokee territory instead of generating a fresh proposal centered on Ball Ground specifically?
2. Check whether there's logic that says "if the searched city falls within an EXISTING claimed territory's footprint, just show that existing territory as a conflict" rather than "generate what a NEW proposal centered on this exact city would look like." If that's the behavior, is it working as intended (showing "already claimed" is reasonable) but mislabeling which cities are actually relevant to it? In other words: confirm whether showing "this is taken" is correct behavior, but check why Ball Ground specifically isn't acknowledged ANYWHERE in the response (not even as "the city you searched falls within this existing territory's coverage area") — the user should at minimum see their searched city named in the conflict explanation, not just see an unrelated list of 4 other cities with no mention of Ball Ground at all.
3. Check if this reveals a FOOTPRINT OVERLAP issue: does the existing Cherokee territory's ZIP footprint geographically extend far enough to swallow Ball Ground's ZIP, even though Ball Ground isn't one of its 4 named hub cities? If so, this is worth flagging clearly — it would mean the existing Cherokee territory's claimed footprint is broader than its 4 listed mailing areas suggest, which has implications for whether Ball Ground could ever be claimed separately by a different dealer in the future.

Report back: the root cause of the wrong Pickens County label, and a clear explanation of why searching "Ball Ground" returned an unrelated-looking territory with no mention of Ball Ground at all — including whether Ball Ground's ZIP is actually already inside the existing Cherokee territory's true footprint (which would be important to know) or whether this is purely a display/labeling bug with no real overlap. Do not apply a fix until both are clearly understood and reported.
