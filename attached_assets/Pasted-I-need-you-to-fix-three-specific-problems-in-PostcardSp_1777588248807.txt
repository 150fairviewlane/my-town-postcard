I need you to fix three specific problems in PostcardSpotPicker.jsx. Do not touch AdGenerator.jsx, industryAssets.js, or any backend code. Only fix these three things in PostcardSpotPicker.jsx and replace PostcardSampleAds.jsx with the new version I am providing.

PROBLEM 1 - AVAILABLE SPOTS FOR PURCHASE. Right now ALL spots are showing sample ads and none are available to purchase. This is wrong. The postcard must have a mix of spots: some showing sample ads as examples of what a purchased ad looks like, and others showing as available green spots that a customer can click to purchase. The rule is: roughly half the spots on the front and half on the back should be available for purchase, shown with a green plus sign and a Reserve This Spot label. The other half show sample ads. Specifically, keep these spots available for purchase and do NOT put sample ads in them: on the front side keep at least one XL spot, one L spot, one M spot, and one S spot available. On the back side keep at least one L spot, one M spot, and two S spots available. Every available spot must still be clickable and open the AdGenerator when clicked.

PROBLEM 2 - SAMPLE ADS MUST ALWAYS HAVE AN IMAGE. Replace PostcardSampleAds.jsx with the new version I am providing. After replacing it, check every spot that shows a sample ad and confirm that an image is rendering inside it. If any sample ad spot is showing a blank or empty area where a photo should be, it means the Unsplash image URL failed to load. In that case add an onError handler to each img tag in PostcardSampleAds.jsx that sets the image src to a solid colored div using the industry primary color as a fallback background, so there is never a blank white or empty area.

PROBLEM 3 - COUPON OVERLAPPING PHOTOS. In the PostcardSampleAds.jsx file I am providing, the PhotoBoldAd component has been rewritten to use flexbox column layout instead of absolute positioning for the bottom coupon section. This means the coupon can no longer overlap the photos. Do not revert this change. If you see any other template where a coupon or text element is overlapping a photo, fix it the same way: replace absolute bottom positioning with a flex column layout that uses flex 1 on the center section and flexShrink 0 on the bottom section.

After making these changes, the postcard grid should look like a real, nearly-full postcard where some spots show beautiful sample ads demonstrating what a purchased ad looks like, and other spots show a clear green available indicator inviting the customer to purchase. The ratio of sample ads to available spots should feel realistic, like a postcard that already has several businesses on it but still has good spots left.