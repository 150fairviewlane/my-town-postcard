Before applying the per-template buildFooterZone fix:

1. BYTE BUDGET CHECK FIRST. buildFooterZone is called by every single template — adding a new per-template corner description AND the QR prohibition sentence to it is a guaranteed addition across all 24 template×orientation combos, not an opt-in. Several templates (purple-sage, sage-organic, heritage-home, home-elegance, wok-fire, brush-stroke) were already at or near the 7,800-byte ceiling earlier tonight after the global QR-ban addition. Run the byte-size check with the new text BEFORE applying broadly, and report which templates go over. If any do, find the bytes in boilerplate (same approach as every previous round tonight) — do NOT shorten the new QR prohibition sentence itself or the per-template color accuracy to make room.

2. DEAD CODE — flag, don't fix tonight, but log a real follow-up ticket. The adGenGrok.ts local refLines/outputRequirements block (lines 671-1434) is the second time this session dead code has actively misled an investigation (AdGenerator.jsx was the first). Recommend either deleting it or adding a prominent "// DEAD CODE — overwritten by buildAdPrompt() at line X, do not edit" comment block at the top, specifically because it ALMOST caused a wasted fix tonight. This is cleanup, not urgent — don't do it now, just don't let it sit there as a trap for the next person (or the next AI session) who reads this file without already knowing the full history.

Apply the qrSlot fix once byte budget is confirmed clean. Then generate 3-4 fresh renders EACH on at least 3 templates with genuinely different accent colors — heritage-home (burgundy, cream, rounded), purple-sage (lavender, dashed), and health-wellness (teal, white, no border) — specifically because these are the ones furthest from the original wrong "gold" assumption and the best test of whether the per-template color matching actually holds. Visually confirm zero fake/duplicate QR codes across all renders, not just one per template — same standing rule as every other fix tonight, since Grok samples probabilistically and one clean render proves nothing.

Report byte-budget results before switching to Build mode.
