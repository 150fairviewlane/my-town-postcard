Build out the QR-detect-and-replace workflow with these specifications — this replaces the original "ask Grok to leave the corner blank" approach for templates using this flow, and should NOT include a second corner-cleanup AI pass (it's unnecessary here and doubles AI cost per ad for no benefit — see reasoning below).

1. PROMPT NUDGE: add an instruction asking Grok to draw its placeholder QR somewhat smaller than its natural default (~185px average observed) — target roughly 140-150px. Treat this as a soft bias that reduces how often the safety margin below needs to do heavy lifting, not as a guarantee.

2. DETECTION: use a vision model call to return the bounding box of the ENTIRE QR-related visual cluster — not just the bare QR shape, but including any backing card, border/frame, or label text directly associated with it. Per the 6-sample probe already run, this cluster always appears as one attached unit that moves together (backing card + label + frame, when present), never as separately-anchored pieces — so detecting the whole cluster in one bounding box is both accurate and sufficient.

3. SIZING/PLACEMENT: composite the real QR card sized relative to the detected cluster bounding box, not a fixed value — target roughly 1.3x the larger dimension of the box, centered on the box's center point. This guarantees full coverage of the detected cluster regardless of whether Grok complied with the size nudge in #1, and handles the bare-QR cases. For clusters where the detected box is unusually large relative to a typical QR (e.g. a prominent separate frame element, like the Mountain View Plumbing sample from the probe), the 1.3x multiplier applied to the FULL cluster box (not just an assumed QR-only sub-region) should already cover the frame too — verify this specifically against that sample.

4. NO SECOND AI PASS: do not add a corner-cleanup refine call to this workflow. It creates ordering conflicts (could delete the cluster before detection, shift pixels after detection invalidating the bbox, or risk corrupting the already-composited real QR if run last) and doubles per-ad AI cost. The cluster-level bounding box detection in #2 replaces its function — it directly identifies and fully covers the decorative elements that cleanup used to catch blindly. Leave the existing cleanup pass untouched for any other templates/flows still using the original blank-corner approach.

Re-run the same 6 samples with this complete pipeline (nudge + cluster detection + 1.3x relative sizing, no second pass) and report: detection success rate, whether the composited real QR fully covers each detected cluster with no visible edges peeking out (check Mountain View Plumbing specifically), and the actual per-ad AI call count/cost compared to the original blank-corner + cleanup approach.
