Fix in SMART_HTML (artifacts/api-server/src/routes/adGenGrok.ts, /api/grok-ad-generator) — verify against the actual current code first, since this file has had several unpushed edits since my last read (image library removal, swatch removal, multiple layout passes). The `onIndustryChange()` function I'm describing may have shifted; locate the current equivalent.

Bug: selecting/changing the Industry dropdown unconditionally overwrites Tagline, Offer, Fine Print, and rebuilds the entire Menu Items list from industry defaults — destroying anything the user already typed into those fields.

Fix: track whether each prefillable field's current content came from auto-fill or from the user actually typing, not just whether it's empty. Approach:
- Add a simple "auto-filled" marker (e.g. a data attribute or a Set of field IDs) for any field that gets set by the industry-default logic.
- Add an `input` event listener on each prefillable field (Tagline, Offer, Fine Print, each menu item input) that clears that field's "auto-filled" marker the moment the user types anything — this permanently marks it as user-edited for the rest of the session.
- In the industry-change handler, only overwrite a field if it is either completely empty OR still marked as auto-filled (never user-edited). Once a field is user-edited, no future industry change should ever touch it again, even if the user clears it back to empty — actually, on reflection: if the user clears it back to empty themselves, that's also a deliberate action, so empty-and-previously-user-edited should also NOT be re-filled. Only fields that have literally never been typed into by the user should be eligible for prefill, regardless of current emptiness.
- Apply this same protection to any other field this function touches that I may not be aware of in the current code (e.g. if swatch/color selection or photo library selection still gets triggered by industry change and those features still exist) — use your judgment based on what's actually still there.

After the fix: test by filling in some fields manually, then changing the Industry dropdown, and confirm manually-entered values survive while empty/untouched fields still get sensible defaults. Then test switching industries a second time before typing anything, and confirm those still-untouched fields refresh to the new industry's defaults. Screenshot or describe the result for both cases.
