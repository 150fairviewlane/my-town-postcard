The `maxZoom: 9` fix is now CONCLUSIVELY confirmed live in production — verified directly in the minified bundle: `v.fitBounds(b.latLngBounds(R),{padding:[40,40],animate:!1,maxZoom:9})`. This is not a caching or CDN-propagation issue — the user has now tested on multiple fresh browsers well after deployment completed, with the same broken result every time.

**This means the bug is no longer "wrong code is deployed" — it's "this exact, confirmed-correct code still produces a broken zoom against real production data."** Stop investigating deployment/caching entirely. Investigate the actual runtime behavior instead.

1. **Fetch the REAL data this `fitBounds` call is actually using right now in production** — call the live production `/api/territories/public` endpoint directly (or whatever endpoint feeds this map) and show the exact raw response: every active territory's name, slug, latitude, and longitude as currently stored in the PRODUCTION database. Do not assume this matches what was tested in dev/preview — show the actual current values.

2. **Check for a bad or duplicate coordinate.** `maxZoom: 9` caps the zoom level Leaflet will use, but `fitBounds` still computes its bounds from the actual lat/long values passed in. If even ONE territory has a null, zero, or wildly incorrect coordinate (e.g. `{lat: 0, lng: 0}`, or a coordinate identical to another point, or a coordinate that's NaN/undefined and silently defaulting to something), the computed bounding box itself could be degenerate (e.g. a single point or a zero-size box) — in which case capping maxZoom wouldn't help, since `fitBounds` might bypass normal zoom-level logic entirely for a zero-area bounding box and just center tightly on that one point.

3. **Check what `R` (the bounds array variable in the minified output) actually contains at render time** — add a temporary console.log of the computed bounds array immediately before the `fitBounds` call (in the actual dev/local source, then verify the same logic against the real production data fetched in step 1) to see exactly what coordinates are being fed in.

4. **Check the label-collision-avoidance logic from earlier this session** — recall that pin positions for clustered territories (Holly Springs, Canton, Ball Ground, Woodstock) were deliberately kept at their EXACT true coordinates, with only LABELS repositioned to avoid overlap. Confirm that `fitBounds` is being called with each pin's actual lat/long (which should differ slightly between the 4 towns), not with some already-clustered/centroid value that could effectively act as a single point.

5. **Test what zoom level `maxZoom: 9` actually produces when the bounding box is very small** (e.g. 4 points within a few miles of each other, as the Cherokee cluster is) — it's possible Leaflet's `fitBounds` behavior with a tiny bounding box and a `maxZoom` cap doesn't behave as expected (e.g. if the box is small enough, Leaflet might still pick a zoom level close to the cap rather than something more zoomed-out, OR there might be a separate issue where the map's `minZoom`/initial zoom default is overriding the capped value in some code path not yet examined).

Report back with the ACTUAL production coordinate data for every active territory (step 1) and a clear explanation of why a confirmed-correct `maxZoom: 9` cap is still producing a tightly zoomed-in single-dot appearance. Do not theorize about caching, CDN propagation, or deployment again — that has been definitively ruled out.
