Revised approach — please implement this instead of modifying buildAdPrompt.ts's template branch logic.

Root cause recap: our normal customer-facing ad generator never sends empty fields to buildAdPrompt (offer, tagline, and menu/services are always populated by the customer). The outreach pipeline is the only caller that sends empty strings for offer/tagline and sometimes an empty array for services — and buildAdPrompt's prompt logic silently omits instructions for those zones when empty, so Grok falls back to whatever the reference template image shows. This is producing two problems: (1) duplicated content across zones (Problem 1), and (2) more seriously, Grok is fabricating a real-looking "20% OFF" discount on the AJ House ad that was never authorized — because the wok-fire template's reference image shows a ticket with a discount on it, and Grok filled it in since no offer text was given to override it.

Instead of touching buildAdPrompt.ts (which is shared with the customer flow and riskier to modify), fix this entirely within generateAdForOutreach.ts by guaranteeing these three fields are NEVER empty before the prompt is built — matching the same invariant the customer flow already relies on.

1. Offer/coupon field — never send empty. Populate with a safe, honest, non-committal default that doesn't fabricate a specific discount or claim the business never made. Use something like "Call for Special Offers" or "Ask About Current Promotions" — never a percentage or dollar amount, since that would be a false claim on the business's behalf. This directly prevents the fabricated "20% OFF" problem seen on the AJ House test ad.

2. Tagline field — never send empty. Populate with a generic but plausible industry-appropriate tagline, e.g. "Locally Owned & Operated" or "Your Local [Industry] Experts" using the business's category. Keep it generic enough to be true for virtually any small business.

3. Menu/services field — guarantee toServiceMenu never returns an empty array. Expand the keyword map to cover the categories that are currently falling through (hospitality: inn, hotel, bed & breakfast, lodge, motel; frozen desserts: ice cream, gelato, frozen yogurt, sorbet; and any other common categories you find missing from a review of the map). For any category that still doesn't match after expansion, add a final generic fallback (e.g. ["Quality Service", "Locally Owned", "Call Today"]) so the function can never return an empty array under any input.

Once these three fields are guaranteed non-empty by the time they reach buildAdPrompt, the existing prompt logic (which already handles populated offer/menu correctly for the customer flow) should naturally produce correct, non-duplicated, non-fabricated results — no changes to buildAdPrompt.ts should be necessary. Confirm this is true once implemented by re-testing AJ House, Southern Seasons Inn, Chicago's Pizza, and Scoops.

Separately, still fix Problem 3 (footer address text overflow) as previously scoped: split the address into street on one line and city/state/zip on the next in buildFooterZone's portrait branch, and render it at a smaller size than the phone number, mirroring the landscape branch's existing two-line split. This is a real bug independent of the empty-field issue and does need a small buildAdPrompt.ts change — confirm whether this could affect the customer flow (it shouldn't, since it only changes how an already-provided address is formatted, not whether content is present) before implementing.

Report back your implementation plan and confirm the non-empty guarantees before writing code.
